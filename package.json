{
  "name": "curio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('build', { recursive: true, force: true })\"",
    "build:frontend": "tsc -p tsconfig.client.json",
    "build:rust": "cargo build --manifest-path backend/Cargo.toml --locked --release",
    "build": "npm run build:frontend && npm run build:rust",
    "typecheck": "tsc -p tsconfig.test.json --noEmit && tsc -p tsconfig.client.json --noEmit",
    "prestart": "npm run build",
    "start": "backend/target/release/curio-server",
    "pretest": "npm run build:frontend && npm run build:tests && cargo build --manifest-path backend/Cargo.toml --locked",
    "test": "cargo test --manifest-path backend/Cargo.toml --locked && node --test build/test/*.test.js",
    "check:rust": "cargo fmt --manifest-path backend/Cargo.toml --check && cargo clippy --manifest-path backend/Cargo.toml --locked --all-targets -- -D warnings",
    "build:tests": "tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.3"
  }
}
