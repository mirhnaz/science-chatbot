[package]
name = "science-chatbot-server"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false
description = "Rust backend migration for the Science Chatbot"

[lints.rust]
unsafe_code = "forbid"
